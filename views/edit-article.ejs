<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet"  type="text/css" href="/main.css" />
    <!--CODES THAT I WROTE START-->
    <title>Edit Article</title>
</head>
<body>
    <h1>Edit Article</h1>
    <h1>Welcome to <%= blogTitle %></h1>
    <p>Written by <%= authorName %></p>
    <form action="/edit-article<%= article.article_id ? '/' + article.article_id : '' %>" method="POST">
        <div>
            <label for="title">Title:</label>
            <input type="text" id="title" name="title" value="<%= article.title %>" required>
        </div>
        <div>
            <label for="body">Body:</label>
            <textarea id="body" name="body" rows="10" required><%= article.content %></textarea>
        </div>
        <div>
            <button type="submit">Save</button>
        </div>
    </form>
    <a href="/author-home">Back to Author Home</a>
    <!--CODES THAT I WROTE END-->
</body>
</html>
